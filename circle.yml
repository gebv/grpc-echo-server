machine:
  services:
    - docker
    

dependencies:
  override:
    - docker info
    - docker version
#    - curl https://glide.sh/get | sh
#    - cd src && glide up
    
test:
  override:
    - echo "run tests"
    
compile:
  override:
    - docker-compose -f build.docker-compose.yml up build
    

deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - ls -la
      - ls -la ./bin
